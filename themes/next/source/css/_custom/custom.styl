// Custom styles.

// 700px，当屏幕宽度 < 1600px
// 900px，当屏幕宽度 >= 1600px

// 修改成你期望的宽度
$content-desktop = 977px

// 当视窗超过 1600px 后的宽度
$content-desktop-large = 1277px

img.{$github-emojis-class-name} {
  display: inline !important;
  height: 1.7em;
  width: 1.7em;
  vertical-align: -0.4em;
  border: none !important;
}

img.github-emoji{
  display: inline !important;
  height: 1.7em;
  width: 1.7em;
  vertical-align: -0.4em;
  border: none !important;
}

// 自定义页脚跳动的心样式
@keyframes heartAnimate {
    0%,100%{transform:scale(1);}
    10%,30%{transform:scale(0.9);}
    20%,40%,60%,80%{transform:scale(1.1);}
    50%,70%{transform:scale(1.1);}
}

.love-heart {
  animation: heartAnimate 1.33s ease-in-out infinite;
}

.with-love {
  margin: 7px;
  text-align: center;
  color: rgb(250, 100, 49);
}

#rewardButton>span {
  line-height: 35px;
}